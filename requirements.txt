# quant-101 — pinned to the scientific stack used to author & verify the notebooks.
# Install: pip install -r requirements.txt
numpy>=1.26
scipy>=1.11
pandas>=2.0
matplotlib>=3.7
statsmodels>=0.14
yfinance>=0.2        # only needed if you re-fetch the real ^TWII series
jupytext>=1.16       # to turn the *.py cell-scripts into .ipynb notebooks
arch>=6.0            # GARCH (Stage 5); statsmodels supplies Markov-switching
# Stage 6 (asset pricing: market model / TSMOM / drift+momentum) uses statsmodels OLS —
# no new dependency. Stage 7 (event study: CAR/BHAR, Boehmer/Kolari-Pynnönen, bootstrap
# placebo) reuses statsmodels + scipy + the Stage-4 stationary bootstrap — also no new dep.
# Later stages may still add:
#   linearmodels (panel factor models, optional)
